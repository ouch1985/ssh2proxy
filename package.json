{
  "name": "ssh2proxy",
  "version": "1.0.0",
  "description": "SSH tunnel to local SOCKS5/HTTP proxy",
  "main": "src/index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test:proxy": "bash scripts/smoke-test.sh"
  },
  "dependencies": {
    "ssh2": "^1.16.0"
  }
}
